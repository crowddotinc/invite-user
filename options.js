
let defaultOptions = {
    url: '/search_users',
    keyDownTimer: 350, //milliseconds
    hiddenFormElementSelector: '#invited-users',
    maxUser: 3, //maybe
    forceFallback: false,
    formSelector: '#createProjectForm',
    source: false, //if has user has or false or False
}

export default defaultOptions;